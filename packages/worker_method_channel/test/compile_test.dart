import 'package:test/test.dart';
import 'package:worker_method_channel/src/src.dart';

Future<void> main() async {
  // todo: write makefile test that detects compilation fail
  test('should compile', () async {
    WebWorkerMethodChannel;
  });
}
